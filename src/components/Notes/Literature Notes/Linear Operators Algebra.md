---
date: 2024-11-04
tags:
  - baby
  - atomic
author: Jorge
---
A [[Linear Map]] is an _automorphism_ if has _inverse_, which exist if and only if $\det(A)\neq 0$, the set of those are $Aut(V)$.

$\mathcal{H(V)}$ is and [[Algebra Over A Field|unital associativity algebra]].

$$\mathcal{H}(V) \cong (Aut(V),+,\cdot , \circ)$$

We know that the _composition_ is distributed on right and left, respect the proper interaction with scalar all goes good. Then _composition_ is a _bilinear operation_ that satisfy the associativity and exist neuter element $id$.

If $dim(V)=n\in \mathbb{N}$, then $dim(\mathcal{H(V)})=n^{2}$.

And like is super useful saw the [[Linear Operator]] like [[Isomorphism matrix and linear maps|a matrix]] is better use them.

Here one must have very careful 

[[General Linear Group]]

Like the Abjoint Operators , the set of bounded linear operators on a Hilbert Space
$H$.
[[Hermitian Operators]]


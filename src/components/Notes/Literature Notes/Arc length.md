---
tags:
  - baby
author: Jorge
date: 2025-02-11 17:46
modified: 2025-08-10 15:15
---
When we treat with [[Real function|real functions]] and [[Vector function over a real variable|vector functions of one variable]] over a closed set we can talk of the _arc length_.

[[discrete arc lengh]]

Y si esta definido sobre una curva es derivable. La prove curvasParametricas se hace usando el teorema de valor medio. Is curious note that a similar procedure is used for [[Line integral like area]]

All relies on, However 

$$
\ell=\int_{a}^{b}\lVert \alpha'(t) \rVert dt 
$$

Now we could define a [[Real function]] $s:[a,b]\to[0,\ell]$, such that.


$$s(t)=\int_{C}dS=\int_{t_{0}}^{t}\lVert \alpha'(\mu) \rVert d\mu 
$$


with $s'(t)=\lVert \alpha'(t) \rVert,\forall t\in[a,b]$ 

Give a [[Path]] his length is.

Reference La salle 135.
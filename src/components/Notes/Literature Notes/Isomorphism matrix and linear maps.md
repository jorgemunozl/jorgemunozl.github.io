---
tags:
  - baby
author: Jorge
date: "2025-02-11 15:31"
---
Give two [[Vector Space]] $V,W$, and choosing two [[Basis Of A Space Vector (Hamel)|basis]] in each one.
The [[Map like the generalization of a function]] $X:Hom(V,W)\to \mathcal{M}_{m\times n}(\mathbb{K})$ is a [[Isomorphism on vectors spaces|isomorphism]].

This is to each [[Matrix - Linear Transformation Idea|matrix correspond a linear map and each map correspond a matrix]], then the [[Dimension of a Vector space|dimensions]] between this two vector space are the same.

The _standard basis_ of $Hom(V,W)$ it is obtained using the inverse map $X^{-1}$. (Example)

Then we could say that multiply two matrices is the same of compose two map?

#### Prove

It's necessary use [[Coordinates Linear Algebra|coordinates]] and define a [[Linear Map]].


$$
[T]_{B}=\begin{bmatrix}
a
\end{bmatrix}
$$

[[Matrix representation of map]]
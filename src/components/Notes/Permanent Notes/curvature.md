---
tags:
  - baby
  - atomic
author: Jorge
date: "2025-04-01 15:16"
---
If we want to measure how much a path curve, How we do it? First we ask what are actually the tools we have, we actually have [[Tangent vector functions]] who tell us what is the tangent of the curve in one point, its derivative tell us how is that change exactly.

And there is something really beauty how is that the [[Arc length parameterization]].


while bigger then more it will change, 


When you have a [[path]] which is 
This is a scalar obtain by:
$$
\kappa(t_{0})=\frac{|\lvert T'(t_{0})| \rvert }{\ell'(t_{0} )}
$$

If it is value pretty small then is flat, in the other hand if it is a pretty big value then is pretty curved, what does it mean the direction?

And one can derivate from a circle.

The vector $\vec{\kappa}(t)=\frac{T'}{\lvert f' \rvert}$ is called curvature vector. Only quit the norm of the numerator. [[Frenet Formula]]
##### Radius of Curvature $\rho(t)$

$$
\rho(t)=\frac{1}{\kappa(t)}
$$

The point $c(t)=f(t)+\rho(t)N(t)$, is the center is the circle created, and with the point and the radius you can know any point in that circle. $N(t)$ the [[Tangent vector functions|Normal]] ,  
